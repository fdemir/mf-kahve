// this is a workaround for typescript. we are declaring the MFEs here so that we can import them in the composer without getting reference error.
declare module "search/*";
declare module "cart/*";
declare module "list/*";
